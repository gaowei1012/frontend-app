import { StyleSheet } from 'react-native'
import { px2dp } from '../utils'
import baseColor from './baseStyle'

const styles = StyleSheet.create({
  drawer: {
    marginTop: px2dp(30),
  }
})

export { styles }