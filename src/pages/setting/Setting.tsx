import * as React from 'react'
import { SafeAreaView, View, Text } from 'react-native'

const Settings = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>设置</Text>
      </View>
    </SafeAreaView>
  )
}

export default Settings
